// frontend/src/components/company/LetterHead.jsx
// This component is now unused. We keep a tiny stub so old imports don't crash.
import React from "react";

const LetterHead = () => null;

export default LetterHead;
